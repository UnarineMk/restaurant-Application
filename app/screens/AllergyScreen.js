import { View, Text, ScrollView } from "react-native";
import React from "react";

const AllergyScreen = () => {
  return (
    <ScrollView>
      <View></View>
    </ScrollView>
  );
};

export default AllergyScreen;
